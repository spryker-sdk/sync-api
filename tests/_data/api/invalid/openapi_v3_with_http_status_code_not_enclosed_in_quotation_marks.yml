openapi: 3.0.0
info:
    title: 'Open API test file'
    description: 'This file uses HTTP status code that is not enclosed in quotation marks. This is not allowed in OAS v3.'
    version: 0.2.3

paths:
    /wrong-status-code-definition:
        post:
            description: FooBar
            responses:
                200:
    /wrong-status-code-definition-2:
        post:
            description: FooBar
            responses:
                300:
                default:

components:
    foo:
